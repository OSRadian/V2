[Login]
; Company ID (auto-focused on page load)
UserId=138017
; Kiosk Username
Kiosk=Kiosk4
; Kiosk Password
Password=Cursor-Fiber4-Polka

[Timing]
; Wait for Edge and the page to fully load, in ms
StartupDelay=14000
; Wait for the camera permissions prompt to appear, in ms
CameraPromptDelay=5000
; Pause after refresh before recentering, in ms
RefreshDelay=1500
; Pause after the recenter click before typing the code, in ms
FieldFocusDelay=200

[ReaderInput]
; Max length of the RFID card code to capture
CardCodeLength=32
; InputHook timeout, in seconds
CaptureTimeout=2

[Click]
; Screen coordinates of the field to click before injecting the code
FieldX=954
FieldY=592

[Log]
; Log file name (kept next to the script unless you give a full path)
LogFile=keystrokes.log